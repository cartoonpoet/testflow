/**
 * NestJS 부트스트랩 자리.
 * 실제 구현은 Gen-Phase 4 Task 4.1 에서 ERDify 규약대로 채운다.
 *
 * ★ NestJS 12 는 순수 ESM 이다. 상대 경로 import 에 .js 확장자를 반드시 붙일 것.
 */
export const API_PORT = Number(process.env["API_PORT"] ?? 4000);
