# 의존성
node_modules/

# 빌드 산출물
dist/
dist-poc/
build/
out/
*.tsbuildinfo

# turbo
.turbo/

# 환경변수 — .env.example 만 커밋한다
.env
.env.*
!.env.example

# 증적 저장소 (ARTIFACT_ROOT 기본값)
# ★ 선행 슬래시 필수. `artifacts/` 라고만 쓰면 어느 깊이든 매칭돼
#   `apps/api/src/modules/artifacts/`(소스 코드)까지 통째로 무시된다(실제로 겪었다).
/artifacts/

# Playwright
playwright-report/
test-results/
blob-report/
.playwright/

# 테스트 커버리지
coverage/

# pnpm
# ★ pnpm-lock.yaml 은 커밋한다(무시 목록에 넣지 말 것).
.pnpm-store/
.pnpm-debug.log*

# 로그
*.log
npm-debug.log*

# 에디터 / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
