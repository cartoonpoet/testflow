# 공급망 검역 — 배포 후 24시간(1440분)이 지나지 않은 버전은 설치하지 않는다.
# yarn 시절의 `npmMinimalAgeGate: 1440` 과 동등한 방어다.
# pnpm 10.16+ 에서 지원하며, 본 레포는 pnpm 10.32.1 을 쓴다.
minimum-release-age=1440

# node_modules 는 pnpm 기본값인 isolated(symlink) 구조를 쓴다.
# hoist 로 덮지 않는다 — 암묵적 의존(phantom dependency)은 명시 의존으로 고친다.
